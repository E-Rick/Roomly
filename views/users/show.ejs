<% include ../partials/header %>

<div class="row">
	<div class="col-lg-4 text-center">
		<div>
			<img src="<%= user.avatar %>" alt="user profile image" class="img-thumbnail" style="border-radius: 50%;">
    </div>
	</div>
  <div class="col-lg-8">
    <h1>Hi, I'm <%= user.firstName + " " + user.lastName %></h1>
    <h4>Joined in <%= user.createdAt.getFullYear() %></h4>
    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Error quasi similique architecto ad modi sunt quaerat possimus, officiis quos. Voluptatibus repellendus exercitationem, id saepe sunt consectetur non quam sapiente assumenda.

		Lorem ipsum dolor sit amet, consectetur adipisicing elit. Error quasi similique architecto ad modi sunt quaerat possimus, officiis quos. Voluptatibus repellendus exercitationem, id saepe sunt consectetur non quam sapiente assumenda.
    <h3><%= user.firstName %>'s listings:</h3>
    <div class="row text-center">
      <% rooms.forEach(function(room){ %>
      <div class="col-lg-6">
        <div class="card shadow">
          <div class="inner">
            <img src="<%= room.image %>" class="card-img-top thumbnails" alt="<%= room.name %>" />
          </div>
          <div class="card-body pt-2">
            <p class="room-type mb-0">Entire Apartment · 1 bed</p>
            <h5 class="card-title"><%= room.name %></h5>
            <% if (room.rating === 0) { %>
            <p><em>No reviews yet.</em></p>
            <% } else { %>
            <span class="fa fa-star checked"></span>
            <span class="fa fa-star <% if (room.rating > 1.5) { %> checked <% } %>"></span>
            <span class="fa fa-star <% if (room.rating > 2.5) { %> checked <% } %>"></span>
            <span class="fa fa-star <% if (room.rating > 3.5) { %> checked <% } %>"></span>
            <span class="fa fa-star <% if (room.rating > 4.5) { %> checked <% } %>"></span>
            <% } %>
            <p class="mt-3"><a href="/rooms/<%= room._id %>" class="btn btn-card btn-primary">More Info</a></p>
          </div>
        </div>
      </div>
      <% });%>
    </div>
	</div>
</div>

<% include ../partials/footer %>